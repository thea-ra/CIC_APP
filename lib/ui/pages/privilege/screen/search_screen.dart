import 'package:flutter/material.dart';

class SearchPrivilege extends StatelessWidget {
  const SearchPrivilege({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('search Pre')),
    );
  }
}
